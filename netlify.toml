[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  CI = "false"

# React Router / SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
