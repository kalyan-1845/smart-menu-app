[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  CI = "false"
  NODE_VERSION = "20"

# This section fixes the "MIME type" and 404 errors
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200